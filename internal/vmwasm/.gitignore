# Fuentes de Lua: MIT de terceros, clonadas pineadas por build.sh (no se
# versionan, CLAUDE.md). El artefacto nu.wasm SÍ se comitea (DM1).
lua-5.4.7/

# Toolchain hermética: build.sh descarga el wasi-sdk oficial (pineado por
# sha256) aquí; no se versiona.
wasi-sdk-*/
wasi-sdk-*.tar.gz
